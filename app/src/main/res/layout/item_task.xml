<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="230dp"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_card_office"
    android:padding="16dp"
    android:layout_marginEnd="12dp"
    android:elevation="4dp"
    android:layout_gravity="center_vertical"
    android:clipToPadding="false"
    android:layout_marginBottom="8dp"
    android:layout_marginTop="8dp">

    <TextView
        android:id="@+id/tvProjectType"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Office Project"
        android:textSize="14sp"
        android:maxLines="1"
        android:textColor="#888"
        android:layout_marginEnd="20dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/ImageViewIcon"
        app:layout_constraintStart_toStartOf="parent" />



    <ImageView
        android:id="@+id/ImageViewIcon"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:padding="8dp"

        app:layout_constraintTop_toTopOf="@+id/tvProjectType"
     app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="@+id/tvProjectType" />

    <TextView
        android:id="@+id/tvProjectTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Grocery shopping app design"
        android:textSize="16sp"
        android:textColor="#000"
        android:textStyle="bold"
        android:maxLines="2"
        android:layout_marginTop="4dp"
        app:layout_constraintTop_toBottomOf="@id/tvProjectType"
        app:layout_constraintStart_toStartOf="parent" />

    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="0dp"
        android:layout_height="6dp"
        android:layout_marginTop="10dp"
        android:progress="60"
        android:layout_marginBottom="5dp"
        android:progressDrawable="@drawable/progress_bar_blue"
        style="?android:attr/progressBarStyleHorizontal"
        app:layout_constraintTop_toBottomOf="@id/tvProjectTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
